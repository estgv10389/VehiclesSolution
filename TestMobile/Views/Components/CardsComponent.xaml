<?xml version="1.0" encoding="utf-8" ?>
<Frame 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="TestMobile.Views.Components.CardsComponent"
    CornerRadius="10"
    Padding="10"
    HasShadow="True"
    BorderColor="LightGray"
    BackgroundColor="White"
    x:Name="ThisFrame"
    >
    <Frame.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding TapCommand, Source={x:Reference ThisFrame}}" CommandParameter="{Binding Vehicle, Source={x:Reference ThisFrame}}" />
    </Frame.GestureRecognizers>
    <StackLayout>
        <Image Source="dotnet_bot.png"
             Aspect="AspectFill"
             HeightRequest="150"
             HorizontalOptions="FillAndExpand" />

        <Label Text="{Binding Model}"
             FontAttributes="Bold"
             FontSize="Medium"
             HorizontalOptions="Center" />

        <Label Text="{Binding Year, StringFormat='Year: {0}'}"
             FontSize="Small"
             HorizontalOptions="Center" />
    </StackLayout>
</Frame>